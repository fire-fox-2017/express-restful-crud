<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Todo</title>
  </head>
  <body>
    <form action="/todo/update/<%= todos.id %>" method="post">
      <table>
        <tr>
          <td>Task</td>
          <td>:</td>
          <td><input type="text" name="title" value="<%=todos.title%>"></td>
        </tr>
        <tr>
          <td>Complete</td>
          <td>:</td>
          <td>        <% if(todos.is_complete == true){%>
                    <input type="checkbox" name="is_complite" value="true" checked>
                  <%}else{%>
                    <input type="checkbox" name="is_complite">
                  <%}%></td>
        </tr>
        <tr>
          <td>Id User</td>
          <td>:</td>
          <td><select name="id_user">
            <option value="<%= todos.id_user %>" selected><%= todos.id_user %></option>
          </select></td>
        </tr>
      </table>
      <button type="submit">Submit</button>
    </form>
  </body>
</html>
